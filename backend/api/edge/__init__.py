# fi_edge - FI Edge Server for Cloud+Edge Mode
# Intercepts and logs all Ollama calls locally
#
# Usage:
#   python -m fi_edge.server           # Start server on port 9200
#   python -m fi_edge.server --port X  # Custom port
#
# For OnlyEdge mode, this becomes the "FI" app (replacement for fi-edge-monitor.ps1)
