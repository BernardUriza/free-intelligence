# Fix for Next.js 14 + pnpm + Tailwind CSS PostCSS loader issue
# Hoist all dependencies so Next.js webpack can find them
shamefully-hoist=true

# Alternative: selective hoisting (less aggressive)
# public-hoist-pattern[]=*tailwind*
# public-hoist-pattern[]=*postcss*
# public-hoist-pattern[]=*autoprefixer*
