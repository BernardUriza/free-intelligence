# Free Intelligence - Production Dependencies
# Updated: 2025-12-28
# RAM requirement: 4GB (sentence-transformers + model ~1.5GB)

# ============================================================================
# Core Dependencies
# ============================================================================
h5py>=3.14.0
structlog>=25.4.0
PyYAML>=6.0.0
pydantic>=2.0.0

# ============================================================================
# Web Framework & API
# ============================================================================
fastapi>=0.115.0
uvicorn[standard]>=0.38.0
python-multipart>=0.0.6
aiohttp>=3.9.0
httpx>=0.25.0

# ============================================================================
# Authentication & Security
# ============================================================================
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
requests>=2.31.0
cffi>=2.0.0
cryptography>=46.0.0
auth0-python>=4.13.0
pyjwt>=2.10.0
email-validator>=2.3.0
dnspython>=2.8.0

# ============================================================================
# LLM Providers
# ============================================================================
# anthropic: Claude API client
# ollama: HTTP client only (server runs on FI Edge via Cloudflare Tunnel)
anthropic==0.71.0
ollama==0.6.0

# ============================================================================
# Data Validation & Processing
# ============================================================================
jsonschema>=4.20.0
python-dateutil>=2.8.2

# ============================================================================
# Integrations
# ============================================================================
py-trello>=0.20.0

# ============================================================================
# Sprint 2-3: Payments, Notifications, QR Codes
# ============================================================================
stripe>=14.0.0
twilio>=9.0.0
qrcode[pil]>=8.0

# ============================================================================
# Database
# ============================================================================
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# ============================================================================
# Monitoring & Utilities
# ============================================================================
psutil>=6.1.1
pyarrow>=14.0.0
filelock>=3.12.0

# ============================================================================
# ML / Embeddings (Memoria Longitudinal Infinita)
# ============================================================================
# PyTorch CPU-only first (NO CUDA - saves ~3GB disk space)
# Install via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then sentence-transformers will use this torch
#
# sentence-transformers: Local embeddings for semantic search
# Model: all-MiniLM-L6-v2 (384-dim, 80MB, no API keys needed)
# RAM usage: ~1.5GB with model loaded
sentence-transformers>=2.2.0
