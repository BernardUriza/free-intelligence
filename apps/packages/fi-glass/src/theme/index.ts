export { glassTheme } from './glass-theme';
// glass-chat visual preset (B3-V1). Opt-in look for the chat shell; the runtime
// CSS ships separately as `fi-glass/glass-chat.css`.
export { glassChatPreset, type GlassChatPreset } from './glass-chat-preset';
