// fi-glass · persona-selector (Californio, element 98).
// A generic, accessible single-select dropdown. Owns the dropdown behavior;
// domain (icons/badges/metadata/links) is injected by slot.
export {
  PersonaSelector,
  type PersonaSelectorProps,
  type PersonaSlotContext,
} from './PersonaSelector';
