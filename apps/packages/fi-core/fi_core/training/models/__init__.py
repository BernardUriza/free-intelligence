"""Reference model classes for ``fi_core.training``.

Submodules:
- ``fi_core.training.models.tiny_gpt`` — compact GPT class (config-driven)
- ``fi_core.training.models.presets``  — factory functions ``tiny_gpt_5m`` + ``tiny_gpt_30m``

Both require ``pip install fi-core[training]`` (pulls torch).
"""
