"""``Trainer`` implementations for ``fi_core.training``.

Submodules:
- ``fi_core.training.trainers.pytorch_trainer`` — PyTorch trainer with
  config-driven optimizer + scheduler, automatic mixed precision, and
  ``.pth`` checkpointing.

Requires ``pip install fi-core[training]`` (pulls torch).
"""
