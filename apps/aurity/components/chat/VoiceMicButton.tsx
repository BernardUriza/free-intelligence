'use client';

/**
 * VoiceMicButton — now provided by fi-glass (Americio, element 95).
 * Re-exported here so existing aurity imports (ChatToolbar, MicrophoneDevicePicker)
 * keep working unchanged. Render is byte-identical (the component was moved verbatim).
 */
export { VoiceMicButton } from 'fi-glass/voice';
