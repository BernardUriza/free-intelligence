'use client';

/**
 * MessageContent — now provided by fi-glass (Plutonio, element 94).
 * Re-exported here so existing aurity imports keep working unchanged.
 * The default markdown rendering is byte-identical to the previous local copy.
 */
export { MessageContent, type MessageContentProps } from 'fi-glass/messages';
