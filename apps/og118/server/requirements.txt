fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27          # TTS provider HTTP client (apps/og118/server/tts.py)
# fi-runner (with the Claude Code backend extra) is installed editable from the
# monorepo path — see README:
#   pip install -e ../../packages/fi-runner[claude]
