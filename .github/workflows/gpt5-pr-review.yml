# =============================================================================
# Azure OpenAI PR Review (GPT-5-mini) - ADVISORY MODE
# This workflow provides AI code review but does NOT block merges
# (GPT-5 has false positives on Python f-string escaping, etc.)
# Powered by Azure Cognitive Services
# =============================================================================
name: AI Code Review (GPT-5)

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to review'
        required: true
        type: number

concurrency:
  group: gpt5-gatekeeper-${{ github.event.pull_request.number || github.event.inputs.pr_number }}
  cancel-in-progress: true

jobs:
  ai-review:
    name: "🤖 AI Gatekeeper"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: GPT-5-mini Reviews and Decides
        id: review
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AZURE_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          PR_NUMBER: ${{ github.event.pull_request.number || github.event.inputs.pr_number }}
        run: |
          set -e

          # Get PR info
          PR_TITLE=$(gh pr view $PR_NUMBER --json title -q '.title')
          PR_BODY=$(gh pr view $PR_NUMBER --json body -q '.body // "No description"' | head -c 2000)
          DIFF=$(gh pr diff $PR_NUMBER 2>/dev/null | head -c 25000 || echo "No diff available")
          FILES_CHANGED=$(gh pr view $PR_NUMBER --json files -q '.files | length')

          echo "::group::Analyzing PR #$PR_NUMBER - $PR_TITLE ($FILES_CHANGED files)"

          # Build prompt as single escaped string
          SYSTEM_MSG="You are a pragmatic AI code reviewer. Focus on REAL bugs that will cause runtime failures. Avoid false positives. Trust the developer's intent for intentional changes."

          USER_MSG="Review this PR for CRITICAL issues only.

          TITLE: $PR_TITLE
          DESCRIPTION: $PR_BODY
          FILES CHANGED: $FILES_CHANGED

          DIFF:
          $DIFF

          BLOCK only for these CRITICAL issues:
          1) SQL injection, XSS, command injection (actual exploitable vulnerabilities)
          2) Hardcoded secrets/API keys (real credentials, not placeholders)
          3) Code that will crash at runtime (syntax errors, undefined variables)
          4) Data loss bugs (DELETE without WHERE, DROP TABLE, etc.)

          DO NOT BLOCK for:
          - Python f-string escaping (double braces {{ produce single braces in output - this is correct)
          - File deletions (assume intentional unless obviously accidental)
          - Code style, best practices, or suggestions
          - CI/CD configuration changes
          - Policy decisions (sending data to APIs, etc.)

          RESPOND WITH THIS EXACT FORMAT:
          VERDICT: APPROVE or BLOCK
          CONFIDENCE: HIGH, MEDIUM, or LOW
          SUMMARY: One sentence summary
          ISSUES: List critical issues or None
          RECOMMENDATION: Brief advice

          Rules: Default to APPROVE. BLOCK only for critical security/crash bugs. When in doubt, APPROVE with concerns noted."

          # Escape for JSON using jq
          SYSTEM_ESCAPED=$(echo "$SYSTEM_MSG" | jq -Rs .)
          USER_ESCAPED=$(echo "$USER_MSG" | jq -Rs .)

          # Call Azure OpenAI (GPT-5-mini only supports temperature=1, so we omit it)
          HTTP_CODE=$(curl -s -w "%{http_code}" -o /tmp/response.json \
            "${AZURE_ENDPOINT}openai/deployments/gpt-5-mini/chat/completions?api-version=2024-08-01-preview" \
            -H "Content-Type: application/json" \
            -H "api-key: $AZURE_KEY" \
            -d "{\"messages\":[{\"role\":\"system\",\"content\":$SYSTEM_ESCAPED},{\"role\":\"user\",\"content\":$USER_ESCAPED}],\"max_completion_tokens\":4000}")

          if [ "$HTTP_CODE" != "200" ]; then
            echo "::warning::Azure API returned $HTTP_CODE - defaulting to APPROVE (fail-open)"
            echo "VERDICT=APPROVE" >> $GITHUB_OUTPUT
            exit 0
          fi

          REVIEW=$(cat /tmp/response.json | jq -r '.choices[0].message.content // empty')

          if [ -z "$REVIEW" ]; then
            echo "::warning::Empty AI response - defaulting to APPROVE"
            echo "VERDICT=APPROVE" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "$REVIEW"
          echo "::endgroup::"

          # Parse verdict
          if echo "$REVIEW" | grep -qiE "VERDICT.*BLOCK"; then
            VERDICT="BLOCK"
          else
            VERDICT="APPROVE"
          fi

          echo "VERDICT=$VERDICT" >> $GITHUB_OUTPUT

          # Post comment
          if [ "$VERDICT" = "BLOCK" ]; then
            HEADER="## :robot: AI Gatekeeper Decision: :x: BLOCKED"
            FOOTER="*This check **BLOCKS** merge. Fix the issues above and push again.*"
          else
            HEADER="## :robot: AI Gatekeeper Decision: :white_check_mark: APPROVED"
            FOOTER="*This check allows merge.*"
          fi

          gh pr comment $PR_NUMBER --body "$HEADER

          $REVIEW

          ---
          $FOOTER
          *Powered by Azure OpenAI GPT-5-mini*"

          echo "::notice::AI Verdict: $VERDICT"

      # AI Gatekeeper is ADVISORY only - does not block merges
      # This prevents false positives from blocking valid PRs
      # (e.g., GPT-5 doesn't understand Python f-string escaping)
      - name: AI Review Complete
        run: |
          if [ "${{ steps.review.outputs.VERDICT }}" = "BLOCK" ]; then
            echo "::warning::AI recommends blocking - review the concerns above"
          else
            echo "::notice::AI approved this PR"
          fi
          echo "✅ AI review complete (advisory only)"
