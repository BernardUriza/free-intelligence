# Free Intelligence Backend Environment Variables

# Audio Storage
AURITY_AUDIO_ROOT=storage/audio

# FFmpeg Binaries
FFMPEG_BIN=ffmpeg
FFPROBE_BIN=ffprobe

# WebM Header Graft (true | false)
# Enable server-side EBML header grafting for WebM chunks
# Only needed if frontend MediaRecorder is used (legacy mode)
# RecordRTC mode doesn't need this (chunks already have headers)
AURITY_ENABLE_WEBM_GRAFT=false
